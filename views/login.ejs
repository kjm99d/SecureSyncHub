<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <!-- MUI 스타일 불러오기 (CDN) -->
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" />
  <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@mui/material@5.0.0/umd/material-ui.production.min.css">
  <style>
    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
      background-color: #f5f5f5;
    }

    .login-container {
      background-color: white;
      padding: 40px;
      border-radius: 10px;
      box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.1);
      display: flex;
      flex-direction: column;
      align-items: center;
      max-width: 400px;
      width: 100%;
    }

    .login-container h1 {
      margin-bottom: 20px;
      color: #3f51b5;
    }

    .login-container .MuiTextField-root {
      margin-bottom: 20px;
      width: 100%;
    }

    .login-container .MuiButton-root {
      width: 100%;
      background-color: #3f51b5;
      color: white;
    }

    .login-container .MuiButton-root:hover {
      background-color: #303f9f;
    }

    .signup-link {
      margin-top: 20px;
      color: #3f51b5;
      text-decoration: none;
    }

    .signup-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="login-container">
    <h1>Login</h1>
    <form action="/login" method="POST">
      <!-- 사용자 이름 입력 필드 -->
      <div class="MuiTextField-root">
        <label for="username">Username</label>
        <input type="text" id="username" name="username" class="MuiInputBase-input" required>
      </div>

      <!-- 비밀번호 입력 필드 -->
      <div class="MuiTextField-root">
        <label for="password">Password</label>
        <input type="password" id="password" name="password" class="MuiInputBase-input" required>
      </div>

      <!-- 로그인 버튼 -->
      <button class="MuiButton-root MuiButton-contained" type="submit">
        Login
      </button>
    </form>

    <!-- 회원가입 링크 -->
    <a href="/signup" class="signup-link">Don't have an account? Sign Up</a>
  </div>

</body>
</html>
